node_modules/
dist/
coverage/
tmp/
# PM 임시 작업자료(작업용 지식·참고 PDF 등) — 저장소 대상 아님
etc/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
# 동봉 코퍼스는 압축본(.gz)만 커밋 — 압축 해제본(data/history.sqlite)은 위 *.sqlite로 제외됨
.DS_Store
*.log
.env
.env.*
!.env.example
# Wrangler 시크릿(code-wiz에서 복제) — 절대 커밋 금지
.dev.vars
.dev.vars.*

# 3rd-party 원자료(국편위 한국사DB) — 라이브러리 git 히스토리 제외. 필요 시 이 줄 삭제.
source/

# Beads / Dolt files (added by bd init)
.dolt/
.beads-credential-key

# Python venv (bge GPU ingest 도구)
.venv/
__pycache__/

# 동봉 임베딩 모델(빌드/재현: scripts/fetch-model.mjs) — 118MB, git 제외. npm 배포엔 files로 포함
/models/

# 추출 버전 마커(런타임 생성)
data/history.sqlite.version

.superpowers/
